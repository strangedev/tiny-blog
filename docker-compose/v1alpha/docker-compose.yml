version: '3.1'

services:
  mongodb:
    image: bitnami/mongodb
    restart: unless-stopped
    volumes:
      - mongodb:/bitnami

  # tiny-blog-ui:

  tiny-blog-backend:
    image: noahhummel/tiny-blog-backend
    restart: unless-stopped
    ports:
      - 3000:8080

volumes:
  mongodb: